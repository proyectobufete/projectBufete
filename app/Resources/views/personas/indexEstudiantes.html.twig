{% extends 'layout.html.twig' %}

{% block title %}
  <h1>Listado de Estudiantes</h1>
{% endblock %}
{% block body %}
<div class="portlet-body">
    <div class="row">
      <div class="col-md-6">
        <div class="btn-group">
                  <a href="{{ path('personas_registro') }}">
                    <button id="sample_editable_1_new" class="btn green">
                      Agregar Estudiante <i class="fa fa-plus"></i>
                    </button></a>
        </div>
      </div>


  <div class="col-md-12 col-sm-12 col-xs-6">
      <table class="table table-bordered ">
        <thead>
          <tr role="row" class="">
            <th>Nombre</th>
            <th>Carné</th>
            <th>Teléfono</th>
            <th>Dirección</th>
            <th>Email</th>
            <th>Opciones</th>
          </tr>
        </thead>
        <tbody>
          {% for persona in estudiantes %}
              <tr>
                <td>{{ persona.nombrePersona }}</td>
                <td>{{ persona.estudiantes.carneEstudiante }}</td>
                <td>{{ persona.telefonoPersona }}</td>
                <td>{{ persona.direccionPersona }}</td>
                <td>{{ persona.emailPersona }}</td>
                  <td>
                    <a href="{{ path('personas_detalle', { 'idPersona': persona.idPersona }) }}" class="btn yellow" >Ver</a>
                    <a href="{{ path('personas_edit', { 'idPersona': persona.idPersona }) }}" class="btn blue ">Editar</a>
                  </td>
              </tr>
          {% endfor %}
        </tbody>
      </table>
  </div>
</div>
</div>
{% endblock %}
