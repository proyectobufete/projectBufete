{% extends 'base.html.twig' %}

{% block body %}
    <h1>Editar Caso</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input type="submit" value="Edit" />
    {{ form_end(edit_form) }}

    <ul>
        <li>
            <a href="{{ path('casos_indexciviles') }}">Regresar a la lista</a>
        </li>
    </ul>
{% endblock %}
