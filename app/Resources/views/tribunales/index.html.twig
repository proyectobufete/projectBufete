{% extends 'layout.html.twig' %}
{% block act2 %}
active open
{% endblock %}

{% block ac4 %}
active
{% endblock %}
{% block title %}
  <h1>Listado de Tribunales</h1>
{% endblock %}
{% block body %}
<div class="portlet-body">
    <div class="row">
      <div class="col-md-6">
        <div class="btn-group">
                  <a href="{{ path('tribunales_new') }}">
                    <button id="sample_editable_1_new" class="btn green">
                      Agregar Tribunal <i class="fa fa-plus"></i>
                    </button></a>
        </div>
      </div>
    </div>

  <div class="id="sample_1_wrapper" class="dataTable_wrapper table-responsive"">
      <table class="table table-striped table-bordered table-hover dataTable no-footer">
        <thead>
          <tr role="row" class="">
            <th>Tribunal</th>
            <th>Estado </th>
            <th>Opciones</th>
          </tr>
        </thead>
        <tbody>
          {% for tribunale in tribunales %}
              <tr>
                  <td>{{ tribunale.tribunal }}</td>
                  <td>{% if tribunale.estadoTribunal %}Activo{% else %}No{% endif %}</td>
                  <td>
                    <a href="{{ path('tribunales_show', { 'idTribunal': tribunale.idTribunal }) }}" class="btn btn-warning" >Ver</a>
                    <a href="{{ path('tribunales_edit', { 'idTribunal': tribunale.idTribunal }) }}" class="btn blue ">Editar</a>
                  </td>
              </tr>
          {% endfor %}
        </tbody>
      </table>
  </div>

</div>
{% endblock %}
